---
.rules-merge-or-main:
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME
    - if: $CI_COMMIT_REF_NAME == "main"

.rules-merge:
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME

.rules-main:
  rules:
    - if: $CI_COMMIT_REF_NAME == "main"

